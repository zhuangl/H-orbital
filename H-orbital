#!/usr/bin/env python3
"""Command-line launcher for the H-orbital package.

This wrapper allows running the tool directly from the repository root:

    ./H-orbital 2 1 0 --mode real --plane z --value 0

It delegates all argument parsing and execution logic to ``h_orbital.cli``.
"""

from h_orbital.cli import main


if __name__ == "__main__":
    main()
